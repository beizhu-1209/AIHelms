services:
  aihelms:
    environment:
      DEBUG: "true"
    command: >
      bash -c "pip install --quiet -e '.[dev]' &&
               uvicorn main:app --reload --host 0.0.0.0 --port 8000"

  db:
    ports:
      - "5432:5432"

  redis:
    ports:
      - "6379:6379"
